## LightHouse Tests for a given URL, default to localhost
* run lighthouse tests using the cli and make comparison reports
* save the previous results as json files and use them to generate reports over time
* report Core Vitals, asset sizes and other useful metrics
* add config for viewport, throttling simulation, urls, and other lighthouse settings

## Bundle sizes
* do a bundle size analysis and save the results for future comparison
* add options in config for size threshold

## Page render
* do tests for SSR page rendering time, save the results for future analysis
* include requests number that hit origin
* add options in config for this